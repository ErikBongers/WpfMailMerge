* rtf mail - no images!!!

* stress test also on work laptop.

* check TODOs.

* Test rtf and html again.

* Test for robustness
  : var linkedData = excelData.LinkedData[fieldIndex]; //key field "sdfsdf" does not exist in linked data. Check this early.
  : Unknown maker %%\n%xxx%INSERT...

* Remove email academiestudent from data.

* Test nested collapses.

* Add %%SET FIELD_SEPARATOR #%%
  > to allow settings set within the template.

* collapse empty table rows.
  > gather fields per row and if no values - collapse the row.

* Remove empty line if no value:
  : {{ some_field -LINE }}

* A shortcut to allow collapse of certain empty lines?
  : %%COLLAPSE LINES%% marks that all lines in a section should be collapsed zhen 
    > shortcut
      : %%LINE%%
      > or shorter
      : %%: 
  > or an option for collapse?
  : %%COLLAPSE EMPTY_LINES ?

* Allow fields in included files?
  > version 37...

* Auto merge: if linked table only has one column: skip it - no additional data. It's probably just a combobox list.

* disable UI fields while running.
  > for now: disable them forever. This way we don't have to reset all the variables and connections.


* Handshaking
  - in DKO3, register local user from clipboard.
  - and register it on cloud.
    > where are cloud functions maintained?
     - backupper
     - mathparser!!!
       > copy code to dko3 project?
         NO!
         make a private project on github to have some minimum security!

* Add a files path to UI and settings.
  > This allows for included filenames without an absolute path.
    > first try the absolute path, then use the specified dir.


* Make pre-loading more robust.
  > Timing !!!
    > check if file path is valid
      > this is currently NOT done!
      > when? while typing?
        > if so, async with a delay of .5 seconds?
          > visual feedback if valid file (a checkmark next to the edit field)
      > as long as not a valid  (Excel)
        > cmb with named ranges should be in a "waiting..." state and disabled.

    > if data def file:
      > once valid (an event for this?): 
        > start checking if content is valid.
          > all files exist,...
            > all excels contained the specified worksheets and/or ranges.
          > if waiting for clipboard: start polling
            > visual clue...



* Data loader
  - data is loaded from one file, either an Excel or a definition file.
  > Build a wrapper (strategy) to load the data based on the file.
    > When an excel is selected: a named range needs to be selected as well.
      > this is loaded async.
      > WHEN? is this loaded?
        > user could be typing a file name...
        > 

Priorities
------------
* UI for strategy:
  - Send html mail (attachments at top, flickering screen)
  - RTF: attachement at bottom (no flickering)


Dko data transfer
-------------------
1. Handshaking
  <machinename>.<username> is sent from an app (MailMerge) to clipboard.
    > when?
      > How does app know that DKO3 wants it?
        1. cloud.isMachineRegistered(name), if yes: done!
        2. if not,...
          > when app starts "waiting for dko3 data. Please send the data from DKO3..."
            a. set username to clipboard.
            b. user goes to DKO3, which detects the clipboard username
            c. DKO3 registeres the username.
            d. DKO3 sends the data (from that username).
2. Data transfer for know machine.
  a. the app waits (polls) for cloud.GetWhateverData(machinename).
    > if not yet available it receives nothing (empty string or HTTP code 204 "No content"?)



Remove power query
-------------------
> Do we really need to join the data in XLS?
  > Dko3Data and Excel info have to be merged.
    > that's 2 sources of data!
    > should MailMerge have 2 input fields?
    > Have a datasource definition file with 
      - named definitions for:
        - "StudentTable": 
          > type "Clipboard": wait for a keyword on first line...
          > type "Url": wait for a filled string.
        - "InfoVestigingen": type "Excel": file name + named range.
        - ...
      - Join definitions
> Hashtables?
> What is needed?
- Dko3Data
    > currently in htlm table format, but can be exported as csv, json,...
    - Vestigingsplaatsen (just the names)
    - Werklijst
- Excel
    - Info Vestigingsplaatsen (TextToInsert, DocToInsert, Attachment(s)
    - Info Klassen
    - Etc,...
> Expand joined table into columns????
  > append the extra row's field names to the original field names.
  > Only N:1 joins!!!
    > no duplicating master rows, please.



Always a backup method
------------------------
* Keep option to paste dko3 data in excel and get it there instead of directly.
* Send mails per class
  OK: Is ALWAYS possible.
  > Without hours per student.

Get LIVE data
--------------
* Option in MailMerge to "wait for data...".
* Option in MailMerge to "get data"
  > only to get ALL rows, not filtered.
    > ...or can we ?
  > Have a data-sources-definition file, where the urls (or commands) for data sources are defined.
    : "https/google-cloude/ebo/.../DkoCommand?GetAllMailMergeData

MailMerge Generic
------------------
* Create a data-definition:
    - Data tables
    - Joins
      > A master table (no hash)
      > For each other table, the 2 join columns
        > this allows the secondary tables to have a hash column.
      > Joined tables should have unique rows to avoid having to duplicate master rows.

* comm with dko3
  > identify machine/connection
  > mailmerge sends command
    : //cloud:getMailMergeData(ALL) or something...
  > or plugin sends it to mailMerge:


>>> https://stackoverflow.com/questions/9327416/how-to-ensure-saving-of-application-state-on-crash-c-sharp

Use stream.Seek(0, SeekOrigin.Begin) or set stream.Position = 0.
Truncate: Immediately call stream.SetLength(stream.Position) to ensure any leftover "trailing" data from the previous version is removed.
To ensure a file is persisted in the event of a crash, you must flush the data through two different layers of buffering: the .NET internal buffer and the Operating System (OS) buffer. 
fileStream.Flush(flushToDisk: true);
If you want every write operation to bypass the OS cache automatically without calling Flush(true) manually every time, you can initialize your stream with the FileOptions.WriteThrough flag. 
var fs = new FileStream(path, FileMode.Open, FileAccess.Write, FileShare.Read, 
                        bufferSize: 4096, FileOptions.WriteThrough);
Use code with caution. Best for: Scenarios where durability is more important than raw write speed. 



in .net , to constantly overwrite a small file, is it best to keep it open, seek the beginning, or is it better to close and open it?
In .NET, if you need to constantly overwrite a small file (e.g., several times per second), the most efficient approach is to keep the file open and seek to the beginning for each write. 

Stack Overflow
Stack Overflow
 +2
Why Keeping it Open is Better
Performance: Opening and closing a file is a heavy operation involving system calls, security checks, and resource allocation. It can take up to 40x longer than the actual read/write operation.
Resource Management: Repeatedly opening and closing creates unnecessary overhead for the OS to update file handles and system locks.
Reliability: Keeping a handle open ensures your process maintains its lock, preventing other applications from intercepting the file between your operations. 
Stack Overflow
Stack Overflow
 +3
Best Practice Implementation
To properly overwrite an open file in .NET, you must handle the case where new data is shorter than the old data:
Seek to the beginning: Use stream.Seek(0, SeekOrigin.Begin) or set stream.Position = 0.
Write new data: Perform your write operation.
Truncate: Immediately call stream.SetLength(stream.Position) to ensure any leftover "trailing" data from the previous version is removed.
Flush (Optional): If data durability is critical (e.g., protection against power loss), call stream.Flush() after each write to force the OS buffer to disk. 
Stack Overflow
Stack Overflow
 +4
When to Close and Reopen
Low Frequency: If you only overwrite the file once every few minutes, use File.WriteAllText(). The performance gain of keeping it open is negligible, and closing it frees the resource for other apps.
External Access: If other programs need to read or modify the file between your updates, you should close the handle to release the lock. 
Experts Exchange
Experts Exchange
 +4
Are you writing this data from a single thread, or do you need to coordinate access across multiple parts of your app?
AI responses may include mistakes. Learn more





undefined
undefined
undefined
16 sites
File I/O Performance Tips - JacksonDunstan.com
04 Jan 2016 — Reading and writing are tremendously more efficient if done in chunks rather than one byte at a time. While you probably won't wri...

Jackson Dunstan

Reusing a filestream - Stack Overflow
22 Feb 2010 — * 5 Answers. Sorted by: 15. Your suspicion is correct - if you reset the position of an open file stream and write content that's ...

Stack Overflow
C# StreamWriter keeping file open?? - Experts Exchange
22 Aug 2008 — You're not dealing with a memory stream, so all the log entries are sitting in memory. So unless . Net has a memory leak issue wit...

Experts Exchange
Show all
How do i make sure the stream is persisted, in case of a crash?
To ensure a file is persisted in the event of a crash, you must flush the data through two different layers of buffering: the .NET internal buffer and the Operating System (OS) buffer. 
Microsoft Dev Blogs
Microsoft Dev Blogs
 +1
1. Flush to Disk
A standard Flush() only moves data from your application into the OS's memory. If the system loses power or the OS crashes, that data is lost. To force it all the way to the physical storage media, use: 
Stack Overflow
Stack Overflow
 +4
csharp
fileStream.Flush(flushToDisk: true);
Use code with caution.

How it works: Passing true tells the OS to flush its intermediate file buffers and "write through" to the hardware.
Performance: This is significantly slower than a standard flush because it waits for the physical disk to acknowledge the write. 
Stack Overflow
Stack Overflow
 +3
2. Use the WriteThrough Option 
If you want every write operation to bypass the OS cache automatically without calling Flush(true) manually every time, you can initialize your stream with the FileOptions.WriteThrough flag. 
Stack Overflow
Stack Overflow
csharp
var fs = new FileStream(path, FileMode.Open, FileAccess.Write, FileShare.Read, 
                        bufferSize: 4096, FileOptions.WriteThrough);
Use code with caution.

Best for: Scenarios where durability is more important than raw write speed. 
Medium
Medium
3. Handle Application-Level Crashes
If the application crashes (but the OS stays up), simply using a using block or calling Dispose() is usually enough. .NET automatically attempts to flush and close the file handle during the disposal process. 
4. Atomic Overwrites (The "Paranoid" Method) 
Even with Flush(true), if a crash occurs during the write, your file might end up half-written (corrupted). For critical data, the standard "Safe Save" pattern is: 
Reddit
Reddit
 +1
Write the data to a temporary file in the same directory.
Call Flush(true) on the temp file.
Use File.Replace() or File.Move(..., overwrite: true) to swap the temp file with the real one.
Note: This is an atomic operation at the OS level; the file will either be the "old version" or the "new version," never a corrupted mix of both. 
Reddit
Reddit
 +1
Are you more concerned about the app crashing, or the entire machine losing power?
AI responses may include mistakes. Learn more





undefined
undefined
undefined
12 sites
Do I need to use FileStream.Flush() or FileStream.Flush(true)?
12 Aug 2014 — * 2 Answers. Sorted by: Calling Dispose , or any kind of Flush , will make it so that other software will see the data that has be...

Stack Overflow
File IO improvements in .NET 6 - Microsoft Developer Blogs
01 Sept 2021 — bufferSize sets the size of the FileStream private buffer used for buffering. When a user requests a read of n bytes, and n is les...

Microsoft Dev Blogs

How does Windows handle a IDisposable class when ... - Reddit
25 Sept 2024 — Edit. A comment reminded me: this is also why for SUPER CRITICAL programs you should not save a new version of a file by just casu...

Reddit
Show all






























* RTF contains the vars!
: {\*\docvar {Dko3Recepient}{acercicek@gmail.com}}{\*\docvar {Dko3Subject}{Inschrijving voor Havin Masal ACER}}
  {\*\docvar {Dko3Attachments}{C:\'5cUsers\'5cerikb\'5cDesktop\'5cDeMuziekschool.pdf}}
  > Alter IStrategy:
    > add Opaquedoc = strategy.OpenDoc();
    > strategy.getAttachments(opaquedoc)
    > .getSubject(opaquedoc)
    > .getRecipients(opaquedoc)



* Use OpenXML sdk?????
  >>> https://stackoverflow.com/questions/56288248/how-to-copy-a-word-document-content-in-another-word-document-including-bookmark
  >>> https://stackoverflow.com/questions/39382093/how-to-copy-the-content-of-one-word-document-into-another-word-document?noredirect=1&lq=1

* Checsk should NOT throw.


  > new file based on template: Word.Document newDoc = wordApp.Documents.Add(templatePath);
  > new file, copying content from template: 
    > AI says: 
        Word.Document newDoc = wordApp.Documents.Add(); newDoc.Content = templateDoc.Content;
    > google says: 
        Word.Range targetRange = targetDoc.Content;
        // Copy with 100% fidelity
        targetRange.FormattedText = sourceDoc.Content.FormattedText; 


Plan B
---------
Every step should be possible manually, so that if the plugin fails, the user can still complete the process.

- Step 0: Setup
  > Create docs per class.
- Step 1: Update word documents for group classes.
  > Update les data from dko3.
    > how? Plugin can't do this.
    > use google cloud functions to pull data from dko3 and update word documents.
      > dko3 cloud interop/rpc:
        > dko3 polls getCommandFor("dko3");
        > dko3 calls setReply(commandId, ...);
      > c# polls getReply(commandId) and updates word documents.

  !! Alternative: manually update the word documents.
  > Also, this step may not be needed if the documents are already up-to-date.

- Step 2: Send to classes.
  > poll dko3 for emails for the class
  > paste content of document into outlook email
  
  !! Alternative: manually copy emails from dko3 to outlook.



* Phases
  0. Fill fields
  > start
  1. Check data
    - merged folder clear/
    - attachments
    - includes
  2. Generate docs
  3. Send docs
  > disable start until all fields filled.
  Below start button show the phases.

* Keep track current state.
  > in a state file, store:
    - which docs have been generated
    - which docs have been sent.
  > on restart, pick up where left off.
    
* add subject to doc variables.

* Close all docs when finished.

* Clear merged folder it if it already contains files?

* Send outlook per generated batch?
